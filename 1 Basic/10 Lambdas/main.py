# Lambda refers to a function without a name.
# It's often used in situations where a simple, short-lived function is required.
# Lambda functions are commonly used for operations such as filtering, mapping, or reducing data.

lam = lambda x, y: x + y

print(lam(1,2))
